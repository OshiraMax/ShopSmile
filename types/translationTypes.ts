export type Translations = {
    LoginScreen: LoginScreenTranslations;
  }

export interface LoginScreenTranslations {
    loginTitle: string;
    email: string;
    password: string;
    login: string;
    loginVK: string;
    register: string;
  }
  
  